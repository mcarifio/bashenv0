#!/usr/bin/env bash

# https://gist.github.com/igrigorik/6666860, I couldn't get an alias to workin'saturday5
xdg-open $(git config remote.origin.url | sed 's/git@\(.*\):\(.*\).git/https:\/\/\1\/\2/')/tree/$(git symbolic-ref --quiet --short HEAD )/$(git rev-parse --show-prefix)

